import { leafletLocation } from '../../../helpers/leaflet-location';
import { module, test } from 'qunit';

module('Unit | Helper | leaflet location');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = leafletLocation(42);
  assert.ok(result);
});
